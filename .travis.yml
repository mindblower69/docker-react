sudo: required
services:
  - docker

before_install:
  - docker build -t jcsolutions/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jcsolutions/docker-react npm run test -- --coverage
